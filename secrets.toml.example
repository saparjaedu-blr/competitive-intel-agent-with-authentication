# .streamlit/secrets.toml
# ⚠️  This file is gitignored — never commit it.
# Copy this template, fill in your values, save as .streamlit/secrets.toml

# ── Google OAuth (for Sign In with Google) ─────────────────────────────────────
# 1. Go to console.cloud.google.com
# 2. Create OAuth 2.0 Client ID → Web Application
# 3. Add Authorised Redirect URIs:
#      http://localhost:8501          (local dev)
#      https://your-app.streamlit.app (deployed)

[google_oauth]
client_id     = "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com"
client_secret = "YOUR_GOOGLE_CLIENT_SECRET"
redirect_uri  = "http://localhost:8501"

# ── Session cookie secret (any long random string) ─────────────────────────────
cookie_key = "change_this_to_a_long_random_secret_string_2026"
